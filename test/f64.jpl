proc main() f64 {
    i: f64 = 3;
    x: f64;
    x = i * 4.0;
    z := foo(45, 3.0);

    arr: [4]f64 = [4, 6, 76, 4];
    arr2 := [cast(f64)4, 32, 8];

    return z + x + arr[2] - arr2[1];
}

proc foo(x: f64, y: f64) f64 {
    return x * y + 30;
}

proc other_foo() f64 {
    return 1.34 + 3;
}

#run main();
