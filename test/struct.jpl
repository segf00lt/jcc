proc main() f64 {
    b: Bob;
    f: float = 38.44;
    c: char;
    i := 123;

    x := f + b.f;
    y := -b.i + i;

    b.tau = 5.23446;
    tau_add_one := get_tau_plus_x_plus_one(b, 4.56);

    foo := 12545;

    return tau_add_one;
}

proc get_tau_plus_x_plus_one(b: Bob, x: f64) f64 {
    return b.tau + x + 1;
}

struct Bob {
    i: int = 54;
    unique_id: u16 = ---;
    f: float = 3.14;
    tau: f64 = ---;
    c: s8;
}

#run main();
